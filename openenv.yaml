entrypoint: >
  sh -c "uvicorn server:app --host 0.0.0.0 --port 7860 &
         sleep 5 &&
         python inference.py"

env:
  API_BASE_URL: ${API_BASE_URL}
  API_KEY: ${API_KEY}
  MODEL_NAME: ${MODEL_NAME}
  HF_TOKEN: ${HF_TOKEN}